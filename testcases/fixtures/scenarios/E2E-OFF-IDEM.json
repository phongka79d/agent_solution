{
  "id": "E2E-OFF-IDEM",
  "title": "TC-E2E-005: five identical execution requests produce exactly one effect (mocked SoR)",
  "document": "e2e/offline.md",
  "layer": "e2e",
  "environment": "offline",
  "priority": "critical",
  "gate": "P0",
  "basis": "blueprint",
  "pair": "E2E-LIVE-IDEM",
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "TC-E2E-005",
    "NFR-003",
    "BR-005",
    "BR-006",
    "NFR-002",
    "SRS-17"
  ],
  "facets": [
    "skill:skill.sales.send_message",
    "event:add_to_cart",
    "channel:EMAIL",
    "stage:EXECUTION",
    "stage:EVIDENCE",
    "kpi:Duplicate Execution",
    "kpi:Failed Execution"
  ],
  "risk": "A retried or redelivered action charges, ships or messages the customer twice because duplicate suppression relied on a cached response instead of a durable effect key.",
  "preconditions": [
    "run_id=RUN-E2E-OFF-IDEM, case_id=E2E-OFF-IDEM, worker=w-biz-e2e; the reservation store and the connector transcript are in-memory and reset per case",
    "the effect_key is deterministic, derived from tenant T1 + skill.sales.send_message + step_index 2 + action_revision 1 + request_id evt_a_cart_add_1; run_id, timestamps and UUIDs are never inputs"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "customer_id": "cust-a",
    "channel": "EMAIL",
    "skill_id": "skill.sales.send_message",
    "step_index": 2,
    "action_revision": 1,
    "request_id": "evt_a_cart_add_1",
    "effect_key": "eff_8c1d0f6a4b2e73915c0d8a6f4b1e2d3c5a7b9e0f1a2b3c4d5e6f70819a2b3c4d",
    "duplicate_requests": 5,
    "dispatch_window_ms": 50,
    "conflict_payload": "same effect_key, different RFC8785 request fingerprint"
  },
  "steps": [
    {
      "action": "Dispatch the identical request five times in rapid succession",
      "expected": "the first submission reserves the effect_key (RESERVED) and dispatches once; the other four return REPLAY or IN_FLIGHT and never reach the connector"
    },
    {
      "action": "Assert on the connector transcript, not on the cache",
      "expected": "the outbound connector transcript contains exactly one dispatch for the effect_key, carrying one provider message id"
    },
    {
      "action": "Crash the worker after the reservation and replay",
      "expected": "the replay recomputes a byte-identical effect_key and finds the open reservation, so the reserve outcome is RECONCILE_REQUIRED instead of a blind re-dispatch"
    },
    {
      "action": "Reconcile by effect_key",
      "expected": "reconciliation asks the provider whether the effect exists, resolves presence exactly once, settles the reservation once and never sends a second message"
    },
    {
      "action": "Attempt an effect_key collision with a different payload",
      "expected": "the reserve outcome is CONFLICT with error IDEMPOTENCY_CONFLICT (HTTP 409): the second payload is rejected and neither the reservation nor the provider state changes"
    },
    {
      "action": "Collect the persisted attempt records",
      "expected": "the audit trail holds one success entry for the effect_key plus an explicit entry for every duplicate attempt, and kpi:Duplicate Execution for the run is 0"
    }
  ],
  "assertions": [
    "the connector transcript shows exactly one external dispatch for the effect_key across five identical submissions (TC-E2E-005)",
    "the second through fifth submissions return the stored receipt from the reservation path and never increment the provider effect count",
    "the replay after the crash reproduces the identical effect_key because run_id, timestamps and random UUIDs are not inputs to it",
    "the collision path is fail-closed: IDEMPOTENCY_CONFLICT is raised and nothing is executed or partially reserved"
  ],
  "forbidden": [
    "dispatching to the provider more than once for the same effect_key",
    "deriving the effect_key from run_id, a timestamp or a random UUID"
  ],
  "evidence": [
    "reservation row history for the effect_key (RESERVED, settled exactly once)",
    "connector transcript with the single dispatch and the provider message id",
    "audit entries for each duplicate attempt plus the reconciliation outcome"
  ],
  "cleanup": [
    "settle or release the case reservation and clear the in-memory reservation store and connector transcript; keep the audit entries for the run"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. The real effect guard, reservation store and retry loop are exercised; only the outbound connector is substituted, and its transcript is the oracle for the effect count.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/04-core-engine-and-orchestrator.md",
    "implement/08-security-governance-nfr.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/customers.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
