{
  "id": "UNIT-sales.create_order-TIMEOUT",
  "title": "skill.sales.create_order timeout — effect-unknown reconciliation",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P2",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "BR-005",
    "BR-006",
    "BR-007",
    "NFR-003",
    "NFR-004",
    "NFR-008",
    "SAL-02",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.sales.create_order",
    "event:checkout",
    "event:purchase",
    "kpi:Failed Execution",
    "stage:EXECUTION",
    "kpi:Failed Execution"
  ],
  "risk": "A hanging dependency is interpreted as success or retried blind, producing phantom business effects for skill.sales.create_order.",
  "preconditions": [
    "isolated namespace run_id=RUN-sales.create_order-TIMEOUT, case_id=UNIT-sales.create_order-TIMEOUT, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.sales.create_order registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "cart_id": "CART-A-0115-01",
    "customer_id": "cust-a",
    "shipping_address": {
      "city": "Taipei",
      "cvs_store_id": "7ELEVEN-TPE-001",
      "recipient": "cust-a"
    },
    "payment_method": "CVS_COD",
    "effect_key": "EK-ORDER-A-0115-01",
    "server_price_quote": "quote_token for SKU-OK @ 900 TWD (10% tier)",
    "caller_agent": "SAL-02",
    "granted_authority": "AUTH-3",
    "adapter_fault": "HANG_PAST_TIMEOUT",
    "adapter_latency_ms": 4500
  },
  "steps": [
    {
      "action": "Arm the adapter to accept then hang",
      "expected": "API-001.OrderConnector accepts the request and never responds; the engine deadline is 4000 ms"
    },
    {
      "action": "Observe the abort and the failure classification",
      "expected": "OrderConnector accepts the order then the response is lost past 4000ms -> execution_status='failed', error.code='DISPATCH_TIMEOUT', error.outcome='UNKNOWN'; effect_key EK-ORDER-A-0115-01 stays RESERVED; reconciliation reads ERP by effect_key, finds exactly one order and commits it; no second ERP order (BR-005, BR-006)"
    },
    {
      "action": "Reconcile the unconfirmed effect by effect_key",
      "expected": "reconciliation queries the provider for the effect_key before any retry and resolves presence/absence exactly once; the reservation moves RESERVED -> COMMITTED or RELEASED once"
    },
    {
      "action": "Replay the identical request",
      "expected": "the replay returns the committed receipt, or performs the single authorised effect if reconciliation proved absence; the provider effect count stays 1"
    },
    {
      "action": "Assert no false success",
      "expected": "no success execution_status and no receipt exists without provider evidence; the customer-visible state is derived from the receipt, not the request"
    }
  ],
  "assertions": [
    "exactly one external effect exists for the effect_key after timeout + reconcile + replay (kpi:Duplicate Execution stays 0, kpi:Failed Execution records the unconfirmed attempt)",
    "the run records error.code='DISPATCH_TIMEOUT' with error.outcome='UNKNOWN' and never stores success for the aborted attempt (NFR-008)",
    "retry_on_timeout=false is honoured: max_retries=1 applies only to non-timeout failures and no blind re-dispatch happens",
    "the reconciliation outcome is written to the audit trail with the effect_key, so a later operator can explain the gap"
  ],
  "forbidden": [
    "blind re-dispatch after a timeout",
    "recording success without a provider receipt"
  ],
  "evidence": [
    "per-attempt execution log: attempt number, latency, classification, adapter outcome (API-001.OrderConnector)",
    "effect_reservations row (effect-bearing skills) or adapter invocation log (effect-free skills) with the case effect_key/idempotency key",
    "audit entry recording the timeout/deadline breach and the reconciliation result"
  ],
  "cleanup": [
    "resolve the reservation by reconciliation, then drop namespace RUN-sales.create_order-TIMEOUT and reset the mock adapter fault injection"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. The adapter is fault-injected to hang; the deadline and retry intervals are driven by the engine's virtual clock, so the case is deterministic.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/sales.md",
    "implement/06-api-and-connectors-spec.md",
    "implement/04-core-engine-and-orchestrator.md",
    "implement/04-core-engine-and-orchestrator.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/orders.json",
    "fixtures/offline/catalog.json",
    "fixtures/offline/customers.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
