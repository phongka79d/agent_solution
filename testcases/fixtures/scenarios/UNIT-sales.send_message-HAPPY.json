{
  "id": "UNIT-sales.send_message-HAPPY",
  "title": "skill.sales.send_message happy path and boundary — consent- and mutex-gated outbound message",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P2",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "SAL-04",
    "SAL-05",
    "BR-004",
    "BR-005",
    "NFR-003",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.sales.send_message",
    "channel:EMAIL",
    "kpi:Duplicate Execution",
    "stage:EXECUTION",
    "memory:Working Memory"
  ],
  "risk": "Double messages, spam and consent violations reach real customers on real channels.",
  "preconditions": [
    "isolated namespace run_id=RUN-sales.send_message-HAPPY, case_id=UNIT-sales.send_message-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.sales.send_message registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "recipient_id": "cust-a",
    "channel": "EMAIL",
    "message_content": {
      "text": "Gio hang cua ban van con, can ho tro dat hang?",
      "quick_replies": [
        "Dat hang",
        "De sau"
      ]
    },
    "effect_key": "EK-MSG-A-0115-01",
    "consent_check": "allowed=true (cust-a, EMAIL, marketing)",
    "caller_agent": "SAL-04",
    "granted_authority": "AUTH-3"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller SAL-02, SAL-04, SAL-05 -> skill.sales.send_message; the registry row exposes required_authority=AUTH-3, allowed_agents=[SAL-02, SAL-04, SAL-05], tool=API-003.CommunicationConnector, timeout_ms=3000, retry_policy={max_retries:2, backoff_multiplier:2.0, retry_on_timeout:False}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter API-003.CommunicationConnector is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "message_id='MSG-A-0115-01', provider_reference='EMAIL-SB-778201', delivered_at=2026-01-15T10:00:00Z; connector invoked exactly once"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "recipient cust-b (marketing opt-out) -> BLOCKED_BY_USER before dispatch, 0 sends; session mutex held by a human operator -> SESSION_EXPIRED/refused, 0 sends; replaying EK-MSG-A-0115-01 -> stored message_id returned, still 1 provider send"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_OUTBOUND_MESSAGE is written with mask=['recipient_id'] and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "exactly one customer-visible message exists per effect_key; retries return the cached receipt rather than re-sending (TC-E2E-005)",
    "the send only happens when the consent verdict allowed=true for the exact (recipient, channel, purpose) triple",
    "while a human holds the session mutex, the AI send is refused so the customer never receives two conflicting answers",
    "adapter API-003.CommunicationConnector invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller SAL-02, skill skill.sales.send_message, decision and evidence card EV_OUTBOUND_MESSAGE into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "sending to a recipient without active consent",
    "inferring delivery from the queue acknowledgement without a provider reference"
  ],
  "evidence": [
    "run record for skill.sales.send_message with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_OUTBOUND_MESSAGE payload, redacted (mask: ['recipient_id'])",
    "adapter call log for API-003.CommunicationConnector: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-sales.send_message-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter API-003.CommunicationConnector substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/sales.md",
    "implement/06-api-and-connectors-spec.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/customers.json",
    "fixtures/offline/consents.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
