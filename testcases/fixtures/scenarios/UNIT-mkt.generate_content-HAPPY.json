{
  "id": "UNIT-mkt.generate_content-HAPPY",
  "title": "skill.mkt.generate_content happy path and boundary — vi-VN Zalo ZNS draft from a compliant brief",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P3",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "MKT-03",
    "SRS-11",
    "NFR-004"
  ],
  "facets": [
    "skill:skill.mkt.generate_content",
    "kb:/brand/voice.md",
    "kb:/marketing/content-guidelines.md",
    "channel:ZALO",
    "stage:ACTION",
    "memory:Organizational Knowledge"
  ],
  "risk": "Injected brief text produces unauthorised, off-brand or promise-making customer content.",
  "preconditions": [
    "isolated namespace run_id=RUN-mkt.generate_content-HAPPY, case_id=UNIT-mkt.generate_content-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.mkt.generate_content registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "campaign_theme": "Bao hanh pin 24 thang cho xe may dien",
    "channel": "ZALO_ZNS",
    "locale": "vi-VN",
    "product_skus": [
      "SKU-OK"
    ],
    "caller_agent": "MKT-03",
    "granted_authority": "AUTH-2"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller MKT-03 -> skill.mkt.generate_content; the registry row exposes required_authority=AUTH-2, allowed_agents=[MKT-03], tool=Core.LLMContentEngine, timeout_ms=5000, retry_policy={max_retries:1, backoff_multiplier:1.0, retry_on_timeout:True}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter Core.LLMContentEngine is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "draft_id=DRAFT-0115-07 persisted in state DRAFT, headline/body_content/cta_text returned, channel_payload.channel_type='ZALO_ZNS' with zalo_zns_template.template_id bound to the tenant-approved template; no claim of a delivery date or a discount"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "campaign_theme of 251 chars -> validation failure before the LLM call; locale='th-TH' -> unsupported-locale refusal; both produce no draft_id and nothing persisted"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_MKT_CONTENT_DRAFT is written with mask=no masked fields and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "the draft is created in state DRAFT and is never dispatchable until an approval row bound to its content id exists (TMKT-05 safety edge)",
    "every price/claim token in the payload is traceable to /product/pricing.md or the ERP quote; the semantic oracle flags a fabricated discount or delivery promise",
    "audit records draft_id, channel_type, model id and latency with no masked field",
    "adapter Core.LLMContentEngine invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller MKT-03, skill skill.mkt.generate_content, decision and evidence card EV_MKT_CONTENT_DRAFT into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "auto-publishing or auto-sending the generated draft",
    "persisting a draft after a prompt-injection refusal"
  ],
  "evidence": [
    "run record for skill.mkt.generate_content with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_MKT_CONTENT_DRAFT payload, redacted (mask: no masked fields)",
    "adapter call log for Core.LLMContentEngine: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-mkt.generate_content-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter Core.LLMContentEngine substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/marketing.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/knowledge.json",
    "fixtures/offline/catalog.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
