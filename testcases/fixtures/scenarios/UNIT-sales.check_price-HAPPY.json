{
  "id": "UNIT-sales.check_price-HAPPY",
  "title": "skill.sales.check_price happy path and boundary — floor-price guard on a 30% discount request",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P2",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "SAL-02",
    "BR-001",
    "BR-002",
    "BR-003",
    "FR-SAL-002",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.sales.check_price",
    "kb:/product/pricing.md",
    "intent:price",
    "kpi:Average Order Value"
  ],
  "risk": "The AI grants an arbitrary discount, destroying margin and pricing integrity (TC-E2E-003).",
  "preconditions": [
    "isolated namespace run_id=RUN-sales.check_price-HAPPY, case_id=UNIT-sales.check_price-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.sales.check_price registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "sku_id": "SKU-OK",
    "customer_id": "cust-a",
    "requested_discount_percent": 10,
    "caller_agent": "SAL-02",
    "granted_authority": "AUTH-3"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller SAL-02, SAL-04 -> skill.sales.check_price; the registry row exposes required_authority=AUTH-3, allowed_agents=[SAL-02, SAL-04], tool=API-001.PricingEngine, timeout_ms=2000, retry_policy={max_retries:3, backoff_multiplier:1.5, retry_on_timeout:True}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter API-001.PricingEngine is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "list_price=1000, final_price=900, p_floor=850 (synthetic tenant configuration), discount_allowed=true, currency='TWD', quote_token=<HMAC-SHA256 signed>, quote_expires_at=2026-01-15T10:10:00Z (TTL 10 min)"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "requested_discount_percent=30 (final 700 < p_floor 850) -> discount_allowed=false and final_price stays 1000; the response never contains a below-floor price; requested_discount_percent=51 -> SCHEMA_VALIDATION_ERROR"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_PRICE_CALCULATION is written with mask=['customer_id'] and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "final_price >= p_floor holds in every response, including the refused-discount path (BR-001, BR-002)",
    "quote_token is HMAC-SHA256 signed with the tenant secret; a tampered token or a token signed under another tenant's secret is refused before it reaches create_order",
    "the quoted number equals the ERP list price minus only the policy-eligible discount; the semantic oracle rejects any figure the PricingEngine never returned",
    "adapter API-001.PricingEngine invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller SAL-02, skill skill.sales.check_price, decision and evidence card EV_PRICE_CALCULATION into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "returning a price the PricingEngine did not produce",
    "disclosing cost/COGS internals or the p_floor derivation to the customer-facing layer"
  ],
  "evidence": [
    "run record for skill.sales.check_price with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_PRICE_CALCULATION payload, redacted (mask: ['customer_id'])",
    "adapter call log for API-001.PricingEngine: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-sales.check_price-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter API-001.PricingEngine substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/sales.md",
    "implement/06-api-and-connectors-spec.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/catalog.json",
    "fixtures/offline/customers.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
