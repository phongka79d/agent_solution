{
  "id": "UNIT-care.manage_case-HAPPY",
  "title": "skill.care.manage_case happy path and boundary — 7-state FSM case lifecycle with reopen",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P1",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "CS-01",
    "FR-CS-002",
    "NFR-002",
    "SRS-14",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.care.manage_case",
    "intent:complaint",
    "stage:OUTCOME",
    "memory:Agent Operational Memory",
    "kb:/customer-care/support-policy.md"
  ],
  "risk": "Cases are silently closed or duplicated, so real complaints are never resolved (PILOT-03).",
  "preconditions": [
    "isolated namespace run_id=RUN-care.manage_case-HAPPY, case_id=UNIT-care.manage_case-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.care.manage_case registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "customer_id": "cust-a",
    "intent": "order_status",
    "priority": "P3",
    "conversation_id": "CONV-A-0115-01",
    "related_order_id": "ORD-A-1",
    "action_type": "CREATE",
    "target_status": "NEW",
    "caller_agent": "CS-01",
    "granted_authority": "AUTH-3"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller CS-01 -> skill.care.manage_case; the registry row exposes required_authority=AUTH-3, allowed_agents=[CS-01], tool=PostgreSQL.CaseManagementStore, timeout_ms=2000, retry_policy={max_retries:3, backoff_multiplier:1.5, retry_on_timeout:False}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter PostgreSQL.CaseManagementStore is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "case_id='CASE-A-0115-01' in status NEW with sla_target_hours=24 (P3), evidence_refs linked to the ERP lookup evidence, assigned_owner=null, updated_at=2026-01-15T10:00:00Z; the walk NEW->CLASSIFIED->ASSIGNED->IN_PROGRESS->WAITING_CUSTOMER->RESOLVED->CLOSED succeeds step by step"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "NEW -> RESOLVED -> INVALID_FSM_TRANSITION (non-retryable), case keeps NEW and no partial write; REOPEN on a RESOLVED case -> IN_PROGRESS preserving case_number, SLA history and evidence with no REOPENED state stored; priority 'P5' -> SCHEMA_VALIDATION_ERROR"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_SUPPORT_CASE is written with mask=['customer_id'] and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "a case is never closed unilaterally: CLOSED is only reachable after RESOLVED and the record keeps Resolution_Summary plus evidence refs",
    "every transition is audit-logged with from/to states and the acting owner, and the 7-state FSM admits no skipped state",
    "reopen preserves the original case_id/case_number, SLA history and all evidence (durable audit, not a new case)",
    "adapter PostgreSQL.CaseManagementStore invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller CS-01, skill skill.care.manage_case, decision and evidence card EV_SUPPORT_CASE into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "overwriting a case with an illegal transition or a partially applied write",
    "closing a case whose customer confirmation/evidence is missing"
  ],
  "evidence": [
    "run record for skill.care.manage_case with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_SUPPORT_CASE payload, redacted (mask: ['customer_id'])",
    "adapter call log for PostgreSQL.CaseManagementStore: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-care.manage_case-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter PostgreSQL.CaseManagementStore substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/customer-support.md",
    "implement/03-database-and-memory-schema.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/customers.json",
    "fixtures/offline/orders.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
