{
  "id": "UNIT-care.track_shipping-HAPPY",
  "title": "skill.care.track_shipping happy path and boundary — CVS carrier tracking with checksum validation",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P1",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "CS-01",
    "FR-CS-001",
    "SRS-10",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.care.track_shipping",
    "intent:shipping",
    "event:product_view",
    "stage:CONTEXT"
  ],
  "risk": "Customers act on invented delivery status (missed CVS pickup, unused refund window).",
  "preconditions": [
    "isolated namespace run_id=RUN-care.track_shipping-HAPPY, case_id=UNIT-care.track_shipping-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.care.track_shipping registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "tracking_number": "7ELEVEN-TW-202601150001",
    "carrier": "SEVEN_ELEVEN_CVS",
    "caller_agent": "CS-01",
    "granted_authority": "AUTH-0"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller CS-01 -> skill.care.track_shipping; the registry row exposes required_authority=AUTH-0, allowed_agents=[CS-01], tool=LogisticsConnector, timeout_ms=2500, retry_policy={max_retries:3, backoff_multiplier:1.5, retry_on_timeout:True}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter LogisticsConnector is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "tracking_number echo, carrier='SEVEN_ELEVEN_CVS', shipping_status='AT_CVS_STORE', events[2] ordered by timestamp with location='Taipei Xinyi' and timestamp=2026-01-14T22:05:00Z"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "tracking_number failing the carrier checksum -> validation failure before the carrier call, no status returned; carrier='PIGEON_POST' -> SCHEMA_VALIDATION_ERROR; carrier reporting an unknown number -> CARRIER_TRACKING_NOT_FOUND (non-retryable) with no scan events fabricated"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_SHIPPING_TRACK is written with mask=no masked fields and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "events[] is chronologically ordered and each event maps to a real carrier scan (timestamp+location), with no synthesised intermediate step",
    "shipping_status is one of PICKED_UP|IN_TRANSIT|AT_CVS_STORE|DELIVERED|RETURNED and agrees with the ERP order status for the same parcel",
    "the case that consumed the tracking data links the carrier payload as evidence",
    "adapter LogisticsConnector invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller CS-01, skill skill.care.track_shipping, decision and evidence card EV_SHIPPING_TRACK into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "fabricating scan events or a delivery ETA for an unknown tracking number",
    "presenting a cached tracking state as live after the carrier read failed"
  ],
  "evidence": [
    "run record for skill.care.track_shipping with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_SHIPPING_TRACK payload, redacted (mask: no masked fields)",
    "adapter call log for LogisticsConnector: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-care.track_shipping-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter LogisticsConnector substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/customer-support.md",
    "implement/06-api-and-connectors-spec.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/orders.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
