{
  "id": "UNIT-care.lookup_order-HAPPY",
  "title": "skill.care.lookup_order happy path and boundary — verified identity order lookup with owner binding",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P1",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "CS-01",
    "FR-CS-001",
    "FR-CS-002",
    "NFR-006",
    "NFR-008",
    "TC-E2E-004"
  ],
  "facets": [
    "skill:skill.care.lookup_order",
    "intent:order_status",
    "kb:/customer-care/support-policy.md",
    "memory:Customer Context",
    "stage:SIGNAL"
  ],
  "risk": "Order data leaks to the wrong person, or the customer is told a false delivery state.",
  "preconditions": [
    "isolated namespace run_id=RUN-care.lookup_order-HAPPY, case_id=UNIT-care.lookup_order-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.care.lookup_order registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "order_identifier": "ORD-A-1",
    "customer_id": "cust-a",
    "verification_reference": "VR-sess-a-1-0115",
    "verification_status": "VERIFIED",
    "caller_agent": "CS-01",
    "granted_authority": "AUTH-0"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller CS-01 -> skill.care.lookup_order; the registry row exposes required_authority=AUTH-0, allowed_agents=[CS-01], tool=API-001.OrderConnector, timeout_ms=2000, retry_policy={max_retries:3, backoff_multiplier:1.5, retry_on_timeout:True}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter API-001.OrderConnector is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "order_id='ORD-A-1', status='SHIPPED', line_items[1] {sku_id:'SKU-OK', quantity:1, unit_price:1000, currency:'TWD'}, total_price=1000, currency='TWD', order_date=2026-01-04T09:12:00Z; the reply uses only these ERP fields"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "order_identifier='ORD-B-1' with a cust-a-bound session -> ORDER_OWNER_MISMATCH before any OrderConnector call, 0 order FACTs; verification_status absent or non-VERIFIED -> IDENTITY_UNVERIFIED; unknown order id -> ORDER_NOT_FOUND and the response never distinguishes 'does not exist' from 'not yours'"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_ORDER_LOOKUP is written with mask=['customer_id'] and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "cust-guest (UNVERIFIED) receives zero order fields for ORD-A-1; only the server-resolved owner reaches the connector",
    "the answer contains a subset of the ERP payload for ORD-A-1: status SHIPPED with amount 1000 TWD; no date/amount/status absent from the response appears",
    "order lookup for cust-a appears nowhere in the cust-b run context (NFR-006)",
    "adapter API-001.OrderConnector invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller CS-01, skill skill.care.lookup_order, decision and evidence card EV_ORDER_LOOKUP into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "revealing another customer's order fields to an unverified session",
    "inventing tracking/ETA values that the ERP or carrier did not return"
  ],
  "evidence": [
    "run record for skill.care.lookup_order with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_ORDER_LOOKUP payload, redacted (mask: ['customer_id'])",
    "adapter call log for API-001.OrderConnector: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-care.lookup_order-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter API-001.OrderConnector substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/customer-support.md",
    "implement/06-api-and-connectors-spec.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/orders.json",
    "fixtures/offline/customers.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
