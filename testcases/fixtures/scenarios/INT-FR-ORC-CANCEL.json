{
  "id": "INT-FR-ORC-CANCEL",
  "title": "Cancellation and rejection stop the run without dispatching the pending action",
  "document": "integration/sales-care-orchestrator.md",
  "layer": "integration",
  "environment": "offline",
  "priority": "critical",
  "gate": "P1",
  "basis": "baseline",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/platform.py",
  "requirements": [
    "BR-007",
    "NFR-007",
    "AUTH-4"
  ],
  "facets": [
    "stage:APPROVAL",
    "entity:Approval",
    "kpi:Failed Execution"
  ],
  "risk": "An operator cancels a risky action but it is dispatched anyway, so the customer receives an unauthorized refund or message.",
  "preconditions": [
    "Tenant T1 with two paused AUTH-4 runs (refund and compensation) in namespace orc-cancel-*",
    "Operator op-7 cancels the first run and rejects the second at 2026-01-15T10:20:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "runs": [
      {
        "run_id": "run-orc-cancel-1",
        "skill": "skill.care.initiate_return",
        "decision": "CANCEL"
      },
      {
        "run_id": "run-orc-cancel-2",
        "skill": "skill.care.issue_retention_offer",
        "decision": "REJECT"
      }
    ]
  },
  "steps": [
    {
      "action": "Cancel the first run and reject the second through the decision endpoint",
      "expected": "the first run reaches the stopped state with CANCELLED and the second with REJECTED, both recorded on the approval row"
    },
    {
      "action": "Observe the pending action and the durable task for both runs",
      "expected": "no dispatch occurs for either run and both tasks stay out of running"
    },
    {
      "action": "Attempt to resume a cancelled run by replaying an approve decision",
      "expected": "the replay is refused as not claimable and still no dispatch occurs"
    },
    {
      "action": "Read the audit trail for both runs",
      "expected": "each run records the operator decision, the reason and the terminal state"
    }
  ],
  "assertions": [
    "Both runs are terminal (stopped) with decisions CANCELLED and REJECTED respectively and zero dispatches",
    "The replay of an approve decision on a cancelled run is refused",
    "Each terminal decision appears once in the audit trail with operator identity"
  ],
  "forbidden": [
    "Dispatch a cancelled or rejected action",
    "Reopen a terminal run without a new inbound signal"
  ],
  "evidence": [
    "Approval rows and task states for both runs",
    "Dispatch-count evidence for both runs plus the audit entries of the terminal decisions"
  ],
  "cleanup": [
    "Delete the run-scoped task and approval rows for orc-cancel-*; keep audit entries"
  ],
  "automation": "Drive the real approval claim path and durable task store with a recording adapter; the oracle is the dispatch count and the terminal state pair.",
  "prerequisites": [],
  "references": [
    "implement/04-core-engine-and-orchestrator.md",
    "implement/03-database-and-memory-schema.md",
    "implement/07-human-command-center-ui.md"
  ],
  "fixtures": [
    "fixtures/offline/platform.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
