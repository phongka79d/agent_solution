{
  "id": "UNIT-sales.search_product-HAPPY",
  "title": "skill.sales.search_product happy path and boundary — catalog search for a waterproof commuter bag",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P2",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "SAL-02",
    "FR-SAL-002",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.sales.search_product",
    "kb:/product/products.md",
    "intent:product_info",
    "stage:CONTEXT"
  ],
  "risk": "Sales quotes a product/price that does not exist in the authoritative catalog.",
  "preconditions": [
    "isolated namespace run_id=RUN-sales.search_product-HAPPY, case_id=UNIT-sales.search_product-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.sales.search_product registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "query": "tui chong nuoc di lam",
    "limit": 5,
    "caller_agent": "SAL-02",
    "granted_authority": "AUTH-0"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller SAL-01, SAL-02 -> skill.sales.search_product; the registry row exposes required_authority=AUTH-0, allowed_agents=[SAL-01, SAL-02], tool=API-001.CatalogConnector, timeout_ms=1500, retry_policy={max_retries:3, backoff_multiplier:1.5, retry_on_timeout:True}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter API-001.CatalogConnector is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "products[1]: {product_id:'PROD-BAG-01', sku:'SKU-OK', name:'Commuter bag', list_price:1000, currency:'TWD', in_stock:true}, total_found=1; only ACTIVE SKUs returned"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "limit=21 -> validation failure (limit<=20); limit omitted -> default 5; query='' -> SCHEMA_VALIDATION_ERROR (minLength 1)"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_CATALOG_SEARCH is written with mask=no masked fields and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "only ACTIVE SKUs (SKU-OK) are returned; SKU-DEAD is excluded because its catalog status is DISCONTINUED",
    "list_price/currency in the response equal the ERP catalog row, so no price is invented by the search layer (BR-001)",
    "the audit records the query string and result_count without PII",
    "adapter API-001.CatalogConnector invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller SAL-01, skill skill.sales.search_product, decision and evidence card EV_CATALOG_SEARCH into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "returning a discontinued or not-on-sale SKU as purchasable",
    "executing SQL/injection tokens embedded in the query"
  ],
  "evidence": [
    "run record for skill.sales.search_product with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_CATALOG_SEARCH payload, redacted (mask: no masked fields)",
    "adapter call log for API-001.CatalogConnector: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-sales.search_product-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter API-001.CatalogConnector substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/sales.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/catalog.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
