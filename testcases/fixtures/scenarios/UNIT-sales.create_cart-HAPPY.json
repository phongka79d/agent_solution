{
  "id": "UNIT-sales.create_cart-HAPPY",
  "title": "skill.sales.create_cart happy path and boundary — all-or-nothing cart mutation with idempotency",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P2",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "SAL-02",
    "SAL-04",
    "BR-005",
    "BR-006",
    "NFR-003",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.sales.create_cart",
    "event:add_to_cart",
    "kpi:Duplicate Execution",
    "stage:EXECUTION"
  ],
  "risk": "Duplicate carts/line items double-charge customers and corrupt conversion metrics.",
  "preconditions": [
    "isolated namespace run_id=RUN-sales.create_cart-HAPPY, case_id=UNIT-sales.create_cart-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.sales.create_cart registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "session_id": "sess-a-1",
    "customer_id": "cust-a",
    "items": [
      {
        "sku_id": "SKU-OK",
        "quantity": 2
      }
    ],
    "idempotency_key": "IK-CART-A-0115-01",
    "caller_agent": "SAL-02",
    "granted_authority": "AUTH-3"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller SAL-02, SAL-04 -> skill.sales.create_cart; the registry row exposes required_authority=AUTH-3, allowed_agents=[SAL-02, SAL-04], tool=API-002.CommerceCartAPI, timeout_ms=2000, retry_policy={max_retries:2, backoff_multiplier:1.5, retry_on_timeout:False}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter API-002.CommerceCartAPI is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "cart_id='CART-A-0115-01', item_count=2, subtotal=2000 TWD, currency='TWD', updated_at=2026-01-15T10:00:00Z; replaying the same idempotency_key returns the identical cart_id and subtotal with no duplicated line items"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "items=[] -> validation failure (empty cart never created); items containing SKU-ZERO -> OUT_OF_STOCK (non-retryable) and the cart is left unchanged (all-or-nothing); same idempotency_key with a different payload -> IDEMPOTENCY_CONFLICT"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_CART_MUTATION is written with mask=['customer_id'] and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "exactly one cart exists for the session after 5 identical submissions; item_count and subtotal are unchanged by replays (BR-005, BR-006)",
    "an out-of-stock item leaves the cart byte-identical to its pre-call state (no partial line items)",
    "cart subtotal uses the ERP list price, and each line keeps price_at_addition for later reconciliation",
    "adapter API-002.CommerceCartAPI invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller SAL-02, skill skill.sales.create_cart, decision and evidence card EV_CART_MUTATION into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "creating a second cart or duplicate line items on retry",
    "completing the cart mutation while an item's stock check failed"
  ],
  "evidence": [
    "run record for skill.sales.create_cart with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_CART_MUTATION payload, redacted (mask: ['customer_id'])",
    "adapter call log for API-002.CommerceCartAPI: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-sales.create_cart-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter API-002.CommerceCartAPI substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/sales.md",
    "implement/06-api-and-connectors-spec.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/catalog.json",
    "fixtures/offline/customers.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
