{
  "id": "UNIT-sales.check_stock-HAPPY",
  "title": "skill.sales.check_stock happy path and boundary — available-to-promise for SKU-OK / SKU-ZERO",
  "document": "unit/skills.md",
  "layer": "unit",
  "environment": "offline",
  "priority": "high",
  "gate": "P2",
  "basis": "blueprint",
  "pair": null,
  "status": "NOT_RUN",
  "source_module": "sources/business.py",
  "requirements": [
    "SAL-02",
    "SAL-04",
    "BR-003",
    "FR-SAL-002",
    "SRS-11"
  ],
  "facets": [
    "skill:skill.sales.check_stock",
    "kb:/product/products.md",
    "intent:stock",
    "event:add_to_cart"
  ],
  "risk": "Customers are sold stock that does not exist, producing failed fulfilments and refunds.",
  "preconditions": [
    "isolated namespace run_id=RUN-sales.check_stock-HAPPY, case_id=UNIT-sales.check_stock-HAPPY, worker=w-biz-unit; no shared mutable session with any other case",
    "skill registry row for skill.sales.check_stock registered with test_cases TC-SKILL-01..05 plus >=1 skill-specific case and asserted registrable before the run",
    "in-memory adapters loaded from the case fixtures for tenant T1 only; frozen clock 2026-01-15T10:00:00Z"
  ],
  "inputs": {
    "tenant_id": "11111111-1111-1111-1111-111111111111",
    "sku_id": "SKU-OK",
    "caller_agent": "SAL-02",
    "granted_authority": "AUTH-0"
  },
  "steps": [
    {
      "action": "Route the call through the orchestrator",
      "expected": "Orchestrator resolves caller SAL-01, SAL-02, CS-01 -> skill.sales.check_stock; the registry row exposes required_authority=AUTH-0, allowed_agents=[SAL-01, SAL-02, CS-01], tool=API-001.InventoryConnector, timeout_ms=3000, retry_policy={max_retries:3, backoff_multiplier:1.5, retry_on_timeout:True}"
    },
    {
      "action": "Invoke with the case input payload",
      "expected": "Input validator (additionalProperties:false) accepts it; adapter API-001.InventoryConnector is invoked exactly once with the tenant binding 11111111-1111-1111-1111-111111111111"
    },
    {
      "action": "Inspect the returned payload",
      "expected": "sku_id='SKU-OK', available_quantity=12, in_stock=true, lead_time_days=2, checked_at=2026-01-15T10:00:00Z"
    },
    {
      "action": "Re-run the boundary/validation probe",
      "expected": "sku_id='SKU-ZERO' -> available_quantity=0, in_stock=false (an honest zero, not stock promised); sku_id='SKU-GHOST' -> SKU_NOT_FOUND (non-retryable); the response never returns availability 0 for an unknown SKU"
    },
    {
      "action": "Collect the evidence and audit trail",
      "expected": "Evidence card EV_INVENTORY_CHECK is written with mask=no masked fields and latency; the run record chains run_id -> skill -> tool -> decision -> execution_status (SRS-17)"
    }
  ],
  "assertions": [
    "a stale cached quantity is never returned when the last successful read is older than the freshness bound; the skill returns a failure instead",
    "SKU-ZERO yields in_stock=false, and the downstream cart/order path refuses to add it (OUT_OF_STOCK) rather than completing a sale",
    "audit records sku_id, warehouse scope and latency with no customer identifiers",
    "adapter API-001.InventoryConnector invocation count is exactly 1 for this run and the skill step ends execution_status='success' with latency recorded",
    "the run audit ties run_id, caller SAL-01, skill skill.sales.check_stock, decision and evidence card EV_INVENTORY_CHECK into one record; no component reports success for another component's work"
  ],
  "forbidden": [
    "claiming stock when the WMS read failed",
    "substituting a previous read's quantity as the current availability"
  ],
  "evidence": [
    "run record for skill.sales.check_stock with the 18 SRS-17 fields and the case run_id",
    "evidence card EV_INVENTORY_CHECK payload, redacted (mask: no masked fields)",
    "adapter call log for API-001.InventoryConnector: invocation count, input digest, latency"
  ],
  "cleanup": [
    "release in-memory namespace RUN-sales.check_stock-HAPPY: drop mock rows created by this case, release any effect reservation, keep audit/evidence rows required by retention"
  ],
  "automation": "Real code under test: skill registry row, authority guard/PEP, input validator, deadline and retry loop, effect_key reservation + reconciliation, evidence/audit writer. Substituted boundary: the external adapter (in-memory for offline, approved sandbox for live). Frozen clock 2026-01-15T10:00:00Z; all waits are condition/deadline waits on the engine's virtual clock, never sleep. Adapter API-001.InventoryConnector substituted; the SLA deadline is observed on the virtual clock.",
  "prerequisites": [],
  "references": [
    "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
    "implement/05-skill-system-specifications.md",
    "plans/modules/sales.md",
    "implement/06-api-and-connectors-spec.md"
  ],
  "fixtures": [
    "fixtures/offline/business.json",
    "fixtures/offline/catalog.json"
  ],
  "srs_source": "De_bai_Xay_dung_He_thong_AI_Agent_Marketing_Sales_CSKH_v0.1.md",
  "generator": "testcases/_generate.py",
  "note": "Generated acceptance specification. Plain specification, not an executable test; no application runtime exists, so the status is NOT_RUN."
}
